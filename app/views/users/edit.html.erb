<h1>Editing User</h1>

<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Change Your Name:" %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label "Change Your Username" %><br>
    <%= f.text_field :username %>
  </div>

  <%= form_for(@team) do |a| %>

  <%# Create 2 label options - for new, edit %>

    <div class="field">
      <%= a.label "Change your team Team:" %><br>
      <!-- <%= select(:team, :id, Team.all.map { |t| [t.name, t.name] }) %> -->
      <ul>
      <% @teams.each do |team| %>
        <%= a.radio_button :id, team.id %>  <%= team.name %><br>
      <% end %>
      </ul>
    </div>

  <div class="actions">
    <%= f.submit && a.submit %>

  </div>
  <% end %>
<% end %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
